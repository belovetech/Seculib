# user can only have one active sessions at a time. If a user tries to create a new session when they already have one active sessions, the oldest session should be deleted. The session_manager.py file contains the SessionManager class that manages user sessions. You need to implement the create_session method to ensure that a user can only have one active sessions at a time. The create_session method should return the session_id of the newly created session. If the user already has one active sessions, the oldest session should be deleted, and a new session should be created. The SessionManager class has the following methods that you can use:
